# Generated by theme-render from the active palette; never edit by hand.
# git-delta's syntax/decoration colours assume a light or dark background. This
# include is pulled in by ~/.gitconfig so delta (in git and in lazygit) follows
# the live terminal theme instead of a hardcoded `dark = true`.
[delta]
    dark = true
    light = false
