* {
    bg:        ${THEME_bg};
    bg-alt:    ${THEME_bg_alt};
    bg-sel:    ${THEME_bg_sel};
    fg:        ${THEME_fg_soft};
    fg-dim:    ${THEME_muted_dim};
    accent:    ${THEME_accent};
    secondary: ${THEME_secondary};
    border:    ${THEME_border};
    urgent:    ${THEME_urgent};
}
