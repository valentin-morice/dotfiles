-- Use graphite (colors/graphite.lua), the palette-rendered dotfiles
-- colorscheme — light/dark follows theme-switch like every other tool.
-- tokyonight/catppuccin stay installed as LazyVim's stock fallbacks.
return {
  { "LazyVim/LazyVim", opts = { colorscheme = "graphite" } },
}
