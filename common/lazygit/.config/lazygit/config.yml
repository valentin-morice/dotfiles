git:
  paging:
    colorArg: always
    # No --dark: delta reads [delta] from git config, which theme-render keeps in
    # sync with the terminal light/dark (see delta-theme.gitconfig include).
    pager: delta --paging=never
