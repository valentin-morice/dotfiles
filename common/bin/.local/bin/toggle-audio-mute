#!/bin/bash
# Waybar VOL-click mute toggle. Delegates to volume-notify so the toast +
# progress bar + mute-LED behaviour matches the XF86AudioMute key exactly (one
# source of truth), instead of silently toggling the sink with no feedback.
exec "$HOME/.local/bin/volume-notify" mute
